<h1>Add an ingredient dose to your cocktail</h1>
<div class="container">
<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.input :description %>
<%= f.association :ingredient%>
<%= f.submit "add a cocktail", class: "btn btn-primary" %>
<% end %>
</div>

